[install.scopes]
"@willebrew" = { token = "$GITHUB_TOKEN", url = "https://npm.pkg.github.com/" }

[test]
coverageSkipTestFiles = true
coveragePathIgnorePatterns = [
  "dist/",
  "node_modules/",
  "Documents/",
  "*/dist/*",
  "*/node_modules/*",
]
